import { Divider } from "@material-ui/core";
import React from "react";

const CustomDivider = () => {
  return (
    <Divider
      style={{
        marginTop: 40,
        marginBottom: 40,
      }}
    />
  );
};
export default CustomDivider;
